'use-strict'

import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  topBar: {
    backgroundColor: 'red',
    position: 'absolute',
    top: 0
  },
  mainBlock: {
    backgroundColor: 'blue',
  }
});
